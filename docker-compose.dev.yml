services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    environment:
      NODE_ENV: development
    command: npm run dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
